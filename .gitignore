# Hinweis: data/settings.json ist die AUSGELIEFERTE Vorlage (Token leer) und
# wird bewusst versioniert. Der echte Token liegt zur Laufzeit unter
# $LBPDATA/plugins/bold2lox/settings.json auf dem LoxBerry, nicht im Repo.

# Python
__pycache__/
*.pyc
.venv/
venv/

# IDE
.idea/
