# Note: data/settings.default.json is the SHIPPED template (empty tokens) and is
# versioned on purpose. The live settings.json with real tokens is created at
# runtime under $LBPDATA/bold2lox/ on the LoxBerry and never lives in this repo.

# Python
__pycache__/
*.pyc
.venv/
venv/

# IDE
.idea/
